# Tauri Desktop App — Production API Base URL
# Used when building the desktop application (npm run build called by tauri).
# Defaults to a locally self-hosted backend (see docker-compose.yml, port 8151).
# Point this at your own self-hosted server before building for a different host.
VITE_API_BASE_URL=http://localhost:8151
